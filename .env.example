# 复制为 .env 并按需修改（.env 不要提交到版本库）
PORT=3000
# 必填：openssl rand -base64 48
JWT_SECRET=
# 首次部署时初始 Owner 密码；留空则随机生成并打印在容器日志中
ADMIN_PASSWORD=
# 数据库与瓦片路径（Docker 部署时指向 /app/data 卷，一般无需修改）
DB_PATH=
TILES_DIR=
# 前面有 nginx/Caddy 反代时设为 1
TRUST_PROXY=0
# Docker Compose 隧道配置
TUNNEL_ID=